<%- include('../includes/head.ejs') %>
<link rel="stylesheet" href="/css/admin.css">
    <link rel="stylesheet" href="/css/main.css">
    <%- include('../includes/navigation.ejs') %>
            <section class="main__container">
                <h1 class="main__title">All Recipes</h1>
                <div class="content__container">
                    <div class="search-form__container">
                        <form class="search-form" action="/admin/recipes-search" method="post">
                            <div class="search-form__group">
                                <input class="main-form__input" name="searchParams" type="text" placeholder="Search..">
                                <input type="hidden" name="CSRFToken" value="<%= csrfToken %>">
                                <button class="btn">Search</button>
                            </div>
                        </form>
                        <% if (recipes.length> 0) { %>
                            <table id="adminRecipesTable">
                                <caption>All Recipes:</caption>
                                <thead>
                                    <tr>
                                        <th>ID</th>
                                        <th>Image</th>
                                        <th>Title</th>
                                        <th>Category</th>
                                        <th>Duration</th>
                                        <th>Rating</th>
                                        <th>Type</th>
                                        <th>Cost</th>
                                        <th>Author</th>
                                        <th>Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% recipes.forEach(recipe=> { %>
                                        <tr>
                                            <td data-label="ID">
                                                <%= recipe._id %>
                                            </td>
                                            <td data-label="Image">
                                                <img loading="lazy" class="table__image" src="<%= recipe.featureImage %>" alt="<%= recipe.title %>">
                                            </td>
                                            <td data-label="Title">
                                                <%= recipe.title %>
                                            </td>
                                            <td data-label="Category">
                                                <%= recipe.category.join(', ') %></td>
                                            <td data-label="Duration"><%= recipe.preparation.duration %></td>
                                            <td data-label="Rating">
                                                <% if (recipe.ratings.length > 0) { %>
                                                    <%= (function() { 
                                                        if (!recipe.ratings || recipe.ratings.length === 0) {
                                                            return 0;
                                                        }
                                                        let sum = 0;
                                                        for (let rating of recipe.ratings) {
                                                            sum += rating.stars;
                                                        }
                                                        const average = sum / recipe.ratings.length;
                                                        return average.toFixed(1);
                                                    })() %>  
                                                <% } else { %>
                                                    No Ratings Yet
                                                <% } %>
                                            </td>
                                            <td data-label="Type"><%= recipe.type %></td>
                                            <td data-label="Cost"><%= recipe.cost %> RSD</td>
                                            <td data-label="User"><a class="main__link" href="/admin/user-details/<%= recipe.author._id %>"><%= recipe.author.username %></a></td>
                                            <td data-label="Actions" class="table__action">
                                                <a href="/admin/edit-recipe/<%= recipe._id %>" class="btn">Edit</a>
                                                <a href="/admin/recept-detalji/<%= recipe._id %>" class="btn">Details</a>
                                            </td>
                                        </tr>
                                    <% }); %>
                                </tbody>
                            </table>
                        <% } else { %>
                            <h1>No Recipes Found!</h1>
                        <% } %>
            <%- include('../includes/pagination.ejs', {currentPage: currentPage, nextPage: nextPage, previousPage: previousPage, lastPage: lastPage, hasNextPage: hasNextPage, hasPreviousPage: hasPreviousPage, pageType: 'recipePage' }) %>
                    </div>
                </div>
            </section>
        </body>
        <%- include('../includes/end.ejs') %>